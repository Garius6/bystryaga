# Implementation Plan: быстряга

**Вход**: `spec.md` + `research.md` (оба в этой директории)
**Основа**: `panos/std/сеть/http.pns` (`Маршрутизатор`/`ОбработчикМаршрута`/
`ОтветСервера`), `panos/std/кодирование/json.pns` (`ИзJSON`/`ВJSON`/
`разобрать_в`/`сериализовать_из`)

## Структура пакета

```
panosiki/быстряга/
├── pan.toml                 # имя = "быстряга", зависимости: http (из panos std, встроенный) не нужен как pan-зависимость — std/ всегда доступен
├── быстряга.pns             # публичный API — реэкспорт из модулей ниже
├── приложение.pns           # Приложение, регистрация типизированных маршрутов (US1)
├── валидация.pns            # интерфейс Валидируемое, ответ_ошибки (400/422 helpers)
├── документация.pns         # OpenAPI/Swagger (US2)
├── middleware.pns           # middleware-цепочка + при_старте/при_остановке (US3)
├── test_приложение.pns      # US1 e2e-тесты (реальный сокет)
├── test_документация.pns    # US2 e2e-тесты
└── test_middleware.pns      # US3 e2e-тесты
```

Разбиение на файлы — по user story, не по "слоям" — это даёт
независимо тестируемые/мёржимые куски (US1 работает и мёржится без
US2/US3 в наличии), тот же принцип, что speckit-фичи в основном
репозитории панос.

## Ключевые типы (детали — `data-model.md`)

- `Приложение` — структура, оборачивает `http.Маршрутизатор` +
  `Массив(МетаданныеМаршрута)` (для US2) + `Массив(Middleware)` (для
  US3) + `Массив(функ() -> Пусто)` (`при_старте`) + аналогично для
  `при_остановке`.
- `Валидируемое` — новый интерфейс (`валидация.pns`).
- `Отклик(О)` — обёртка над `О` с явным статус-кодом (FR-007).
- `МетаданныеМаршрута` — путь, метод, (опционально) схемы тела/ответа
  для `/openapi.json`.
- `Middleware = функ(ЗапросМаршрута, Далее) -> ОтветСервера`, `Далее =
  функ(ЗапросМаршрута) -> ОтветСервера`.

## Фазы (следуют приоритету user story из spec.md)

### Фаза 0 — скаффолдинг пакета
- `pan init` в `panosiki/быстряга/` (`имя = "быстряга"`, `версия =
  "0.1.0"`, `точка_входа` не нужна — это библиотека, не
  CLI-приложение, симметрично `коллекциям`/`слогу` в `panos/std/`, не
  `gitsync`).
- `быстряга.pns` — пустой файл-реэкспорт (наполняется по мере
  готовности остальных файлов).
- `git init`, `LICENSE` (см. Open Question ниже), первый коммит.

### Фаза 1 — US1: типизированные обработчики + авто-валидация (P1)
1. `валидация.pns`: интерфейс `Валидируемое`, helper `ответ_ошибки(
   статус: Число, сообщение: Строка) -> ОтветСервера` (JSON-тело
   `{"ошибка": "..."}"`, переиспользуется и для 400, и для 422 — не
   плодить два похожих helper'а).
2. `приложение.pns`: тип `Приложение`, конструктор `приложение()`,
   `.получить[Ответ: json.ВJSON](путь, обработчик: функ(ЗапросМаршрута)
   -> Ответ)` (GET без тела — обёртка сериализует ответ, но не трогает
   JSON-разбор входа вообще, см. FR-005: для GET/DELETE тело в
   принципе не в сигнатуре — `Тело`-параметр там просто НЕ существует,
   а не типизирован как `Пусто`; это ЧИЩЕ, чем FR-005 в исходном виде
   — пересмотрено здесь, задокументировать расхождение в spec.md
   Assumptions при следующем проходе).
3. `.отправить_пост[Тело: json.ИзJSON + Валидируемое, Ответ: json.
   ВJSON](путь, нулевое_тело: функ() -> Тело, обработчик: функ(Тело)
   -> Ответ)` — и симметрично `.отправить_put`/`.отправить_patch`.
   `.удалить[Ответ]` — как `.получить`, без тела.
4. `.запустить(это: Приложение, порт: Число, процессов: Число) ->
   Результат(Число, Ошибка)` — тонкая обёртка над `http.обслуживать(
   порт, это.маршрутизатор.как_обработчик(), процессов)` (плюс US3's
   `при_старте`/`при_остановке`/middleware — но ЭТА фаза может
   заглушить их как no-op, реальная реализация в Фазе 3).
5. `test_приложение.pns` — реальный сокет-тест (та же техника, что
   `tests/wasm/aot_closures_test.zig`'s wasmtime-инвок — здесь проще,
   panos-код на panos-код, без WASM): `запусти это.запустить(порт,
   1)`, `сеть.http_запрос` с клиента на `127.0.0.1:порт`, проверка
   ответа. Все 5 Acceptance Scenarios из US1 spec.md как отдельные
   тесты.

### Фаза 2 — US2: OpenAPI/Swagger (P2)
1. `data-model.md`'s `МетаданныеМаршрута`/`ПолеСхемы` — регистрация
   Фазы 1 (`.отправить_пост` и т.д.) дополняется: КАЖДЫЙ вызов
   регистрации добавляет запись в `это.маршруты_докс` ПОМИМО
   регистрации в `http.Маршрутизатор` — путь/метод берутся из тех же
   аргументов, что уже передаются.
2. `ОписаниеСхемы` — опциональный интерфейс, `документация.pns`
   пытается вызвать `.поля_схемы()` через отдельную non-generic
   проверку УЖЕ НА ЭТАПЕ РЕГИСТРАЦИИ — то есть регистрация
   `.отправить_пост` требует ДВЕ версии (со схемой / без), т.к. и
   здесь та же проблема "нет условной проверки реализации интерфейса
   в рантайме" (см. research.md Вопрос 3) — НО тут это ок оставить
   опциональным, раз это не блокирует основной путь (FR-010 явно
   разрешает деградацию до "путь есть, схемы нет"): дать
   `.отправить_пост_со_схемой[Тело: ... + ОписаниеСхемы, ...]` как
   ДОПОЛНИТЕЛЬНУЮ, не обязательную функцию поверх обычной
   `.отправить_пост`.
3. `документация.pns`: `быстряга.документация(это: Приложение) ->
   http.Обработчик` — строит `json.Значение` для OpenAPI 3.x структуры
   из `это.маршруты_докс`, отдаёт на `/openapi.json` через
   `json.сериализовать`; `/docs` — статическая HTML-строка (Swagger UI
   CDN `<script src="https://unpkg.com/swagger-ui-dist.../swagger-ui-bundle.js">`,
   указывает `url: "/openapi.json"`).
4. `test_документация.pns` — Acceptance Scenarios US2.

### Фаза 3 — US3: middleware + lifecycle (P2)
1. `middleware.pns`: `Middleware`/`Далее` типы, `.middleware(это:
   Приложение, м: Middleware) -> Пусто` (добавляет в `это.
   middleware_список`).
2. Композиция цепочки — в `.как_обработчик()`-эквиваленте
   `Приложение` (не в `http.Маршрутизатор` — тот не знает о
   middleware вообще): сворачивает `middleware_список` СПРАВА НАЛЕВО
   в одну функцию `Далее`, где самый внутренний `Далее` — это уже
   `http.Маршрутизатор.обработать`.
3. `.при_старте(это: Приложение, ф: функ() -> Пусто)`/`.при_остановке(
   ...)` — просто накопление в массивы, вызываются в `.запустить`
   ДО/ПОСЛЕ `http.обслуживать(...)` (`при_остановке` — задокументированное
   ограничение из spec.md Assumptions: `http.обслуживать` сегодня
   блокирующий и обычно не возвращается штатно).
4. `test_middleware.pns` — Acceptance Scenarios US3.

## Тестовая стратегия

Все e2e-тесты — РЕАЛЬНЫЕ сокеты (`http.обслуживать` в отдельном
`запусти`-процессе + `сеть.http_запрос` с клиентской стороны того же
теста), не in-memory вызов обработчика напрямую — тот же принцип, что
`panos/tests/`: "проверено через реальный запуск, не через чтение
кода". `pan test` (существующий раннер, `std/тест.pns`) — стандартный
инструмент, ничего нового не нужно.

## Open Questions (решить до Фазы 0 или в её процессе)

1. **Лицензия**: у соседних пакетов в `panosiki/` разные лицензии
   (`gitsync`/`скобки` — MPL-2.0, порт стороннего кода; `cli`/
   `v8storage` и т.п. — предположительно MIT/без строгих условий, не
   проверено). `быстряга` — не порт, оригинальный код: разумный
   дефолт MIT, подтвердить с пользователем перед первым коммитом.
2. **Версия panos в `pan.toml`**: минимальная версия, где точно
   работает `[T: A + B]` bound-конъюнкция и `Запрос.тело()` — обе
   фичи уже есть в текущем `main` панос (проверено чтением исходников
   этой сессии), но `pan.toml`'s `версия_паноса` должна ссылаться на
   РЕАЛЬНО СУЩЕСТВУЮЩИЙ тег, не на `main` — уточнить актуальный
   тег/версию панос при Фазе 0.
