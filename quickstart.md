# Quickstart: быстряга

Минимальный TODO-API — эквивалент классического FastAPI-tutorial
примера, показывает весь публичный API US1+US2+US3. Прогнан целиком
(реальный `curl` против запущенного `панос`-скрипта) — не иллюстрация.

`импорт быстряга` ниже работает как есть, если этот файл лежит рядом с
`быстряга.pns` (например, при работе внутри самого репозитория). После
`pan add https://github.com/Garius6/bystryaga` зависимость раскладывается
в `модули/быстряга/`, так что импорт становится `импорт
"модули/быстряга/быстряга.pns" как быстряга` — проверено вживую
(`pan add` + реальный импорт из отдельного scratch-проекта).

```
импорт быстряга
импорт "сеть/http" как http
импорт "кодирование/json" как json
импорт ввод_вывод

// --- Тело запроса: создание задачи ---

тип ЗадачаВвод = структура
	текст: Строка
конец

// быстряга.ТелоJSON — авто-разбор JSON в это поле, НЕ json.ИзJSON
// напрямую (структурно то же самое, но разные типы с точки зрения
// тайпчекера — см. doc-комментарий быстряга.pns).
реализация быстряга.ТелоJSON для ЗадачаВвод
	функ заполнить_из_json(это: ЗадачаВвод, значение: json.Значение) -> Опция(Ошибка)
		пер поле_текст = значение.поле("текст")
		если поле_текст.пусто() тогда
			возврат Опция.Есть(Ошибка("json", "ожидалось поле 'текст'"))
		конец
		это.текст = поле_текст.получить(json.Значение.Ноль).как_строку().получить("")
		Опция.Нет()
	конец
конец

// быстряга.Валидируемое — пустой текст задачи недопустим.
реализация быстряга.Валидируемое для ЗадачаВвод
	функ проверить(это: ЗадачаВвод) -> Опция(Строка)
		если длина(это.текст) == 0 тогда
			возврат Опция.Есть("текст не может быть пустым")
		конец
		Опция.Нет()
	конец
конец

// --- Ответ: созданная задача ---

тип Задача = структура
	ид:   Число
	текст: Строка
конец

// быстряга.ОтветJSON — авто-сериализация ответа.
реализация быстряга.ОтветJSON для Задача
	функ в_json(это: Задача) -> json.Значение
		json.объект(массив(
			("ид", json.Значение.Число(это.ид)),
			("текст", json.Значение.Строка(это.текст))
		))
	конец
конец

// --- Приложение ---

функ старт() -> Пусто
	пер апи = быстряга.приложение()

	// Логирующий посредник — оборачивает ВЕСЬ конвейер (включая
	// сопоставление маршрута), не только конкретный обработчик.
	апи.посредник(функ(зпр: Запрос, дальше: быстряга.Далее) -> http.ОтветСервера
		дальше(зпр)
	конец)

	апи.при_старте(функ() -> Пусто
		ввод_вывод.печать("сервер стартует")
	конец)

	// Тело/Ответ generic-параметры НЕ указываются явно в скобках —
	// panos не поддерживает `.метод[Тип](...)` синтаксис на МЕТОДАХ
	// (только на свободных функциях), выводятся из типов аргументов
	// (нулевое_тело/обработчик) автоматически.
	апи.отправить_пост(
		"/задачи",
		функ() -> ЗадачаВвод ЗадачаВвод("") конец,   // нулевое_тело
		функ(ввод: ЗадачаВвод) -> быстряга.Отклик(Задача)
			быстряга.Отклик(201, Задача(1.0, ввод.текст))
		конец
	)

	// Обработчик без тела возвращает ГОЛОЕ Ответ (реализующее
	// быстряга.ОтветJSON), не Массив — список типов не реализует
	// интерфейс за своих элементов.
	апи.получить(
		"/задачи",
		функ(зм: http.ЗапросМаршрута) -> Задача
			Задача(0.0, "")   // упрощённо — в реальном приложении читает из хранилища
		конец
	)

	// US2: /openapi.json + /docs бесплатно, из уже зарегистрированных маршрутов
	апи.маршрутизатор.получить("/openapi.json", быстряга.документация(апи))
	апи.маршрутизатор.получить("/docs", быстряга.swagger_ui())

	апи.запустить(8080, 1.0)
конец
```

## Что происходит на `POST /задачи` с телом `{"текст": ""}`

1. `быстряга` разбирает JSON в `ЗадачаВвод` через `.заполнить_из_json()`.
2. Вызывает `.проверить()` — текст пуст, возвращает `Опция.Есть(...)`.
3. Ответ `422 {"ошибка": "текст не может быть пустым"}` — обработчик
   (лямбда с `Отклик(201, ...)`) НЕ вызывается.

Проверено: `curl -X POST http://127.0.0.1:8080/задачи -d '{"текст": ""}'`
-> `422 {"ошибка":"текст не может быть пустым"}`.

## С валидным телом `{"текст": "купить хлеб"}`

1. Разбор + валидация проходят.
2. Обработчик вызывается с `ЗадачаВвод{текст: "купить хлеб"}`.
3. Возвращает `Отклик(201, Задача(1.0, "купить хлеб"))`.
4. `быстряга` отвечает `201`, `Content-Type: application/json`, тело —
   сериализованный через `.в_json()` результат.

Проверено: `curl -X POST http://127.0.0.1:8080/задачи -d '{"текст": "купить хлеб"}'`
-> `201 {"ид":1,"текст":"купить хлеб"}`.

## `GET /задачи` и `GET /openapi.json`

Проверено: `curl http://127.0.0.1:8080/задачи` -> `200
{"ид":0,"текст":""}`; `curl http://127.0.0.1:8080/openapi.json` ->
валидный OpenAPI 3.x JSON с обоими путями/методами.

## Per-route авторизация: `получить_с` + `jwt_извлечь` + `вместе`

`.посредник(...)` (выше) закрывает ВЕСЬ конвейер разом. Если нужен
доступ к типизированным claims в КОНКРЕТНОМ маршруте — `получить_с`
берёт экстрактор (`Запрос -> Результат(Т, Ошибка)`) и передаёт
результат обработчику вторым параметром; `вместе(а, б)` склеивает
несколько экстракторов в один, возвращающий кортеж — так на один
маршрут вешаются, например, JWT-проверка И произвольный заголовок
одновременно.

```
импорт быстряга
импорт "сеть/http" как http
импорт "кодирование/json" как json

конст СЕКРЕТ = "мой-секрет"

тип Claims = структура
	sub: Строка
конец

// jwt_извлечь[Т: быстряга.ТелоJSON] — claims декодируются в Т тем же
// интерфейсом, что и тела POST-запросов (см. ЗадачаВвод выше).
реализация быстряга.ТелоJSON для Claims
	функ заполнить_из_json(это: Claims, значение: json.Значение) -> Опция(Ошибка)
		это.sub = значение.поле("sub").получить(json.Значение.Ноль).как_строку().получить("")
		Опция.Нет()
	конец
конец

// Свой экстрактор — обычная функция Запрос -> Результат(Т, Ошибка),
// jwt_извлечь ничем не отличается от этой с точки зрения получить_с.
функ извлечь_клиента(зпр: Запрос) -> Результат(Строка, Ошибка)
	пер заголовок = зпр.заголовок("X-Клиент")
	если заголовок.пусто() тогда
		возврат Результат.Неудача(Ошибка("x", "нет X-Клиент"))
	конец
	Результат.Успех(заголовок.получить(""))
конец

тип Профиль = структура
	sub:    Строка
	клиент: Строка
конец

реализация быстряга.ОтветJSON для Профиль
	функ в_json(это: Профиль) -> json.Значение
		json.объект(массив(
			("sub", json.Значение.Строка(это.sub)),
			("клиент", json.Значение.Строка(это.клиент))
		))
	конец
конец

функ старт() -> Пусто
	пер апи = быстряга.приложение()
	апи.получить_с(
		"/профиль",
		быстряга.вместе(
			быстряга.jwt_извлечь(СЕКРЕТ, функ() -> Claims Claims("") конец),
			извлечь_клиента
		),
		функ(зм: http.ЗапросМаршрута, данные: (Claims, Строка)) -> Профиль
			Профиль(данные.0.sub, данные.1)
		конец
	)
	апи.запустить(8080, 1.0)
конец
```

Оба экстрактора должны пройти, иначе — `401` с сообщением первого же
провалившегося (jwt_извлечь проверяется первым, извлечь_клиента —
вторым). Обработчик получает данные ТОЛЬКО когда оба успешны.

Проверено: с валидным `Authorization: Bearer <JWT>` и `X-Клиент: веб`
-> `200 {"sub":"иван","клиент":"веб"}`; без `Authorization` (или без
`X-Клиент`) -> `401`.
